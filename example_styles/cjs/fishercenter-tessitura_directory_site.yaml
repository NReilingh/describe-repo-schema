# Go to https://repo.tools/yaml or something I don't know

version: 0.1:cjs

exports:
  - type: html
    path: dist/
    requires: npm
    build: npx webpack
  - type: bitbucket-pipelines.yml
  - type: appspec.yml
    requires:
        - npm
    
